---
layout: default
title: "2048 — Wanjaaro"
description: "Play the classic 2048-style number merging game directly in your browser."
permalink: /2048
category: puzzles
sidebar: true
sidebar_title: "2048"
sidebar_subtitle: "Merge tiles to reach 2048"
sidebar_icon: "🔢"
---
<div class="benchmark-container"><div class="benchmark-hero"><span class="benchmark-badge playable">Puzzle Game</span><h1>2048</h1><p>Use the arrow keys or swipe to merge matching numbers and build larger tiles.</p></div><div class="tool-panel" style="max-width:520px;margin:auto;text-align:center"><div class="test-stats-bar"><div class="test-stat-item"><div class="test-stat-label">Score</div><div class="test-stat-val" id="score">0</div></div><div class="test-stat-item"><div class="test-stat-label">Best Tile</div><div class="test-stat-val" id="best">2</div></div></div><div id="board" tabindex="0" style="display:grid;grid-template-columns:repeat(4,1fr);gap:7px;max-width:420px;margin:20px auto;outline:none"></div><button id="start" class="btn btn-primary">New Game</button></div></div>
<script>(function(){var b=document.getElementById('board'),sc=document.getElementById('score'),best=document.getElementById('best'),start=document.getElementById('start'),a=[];function add(){var e=[];a.forEach(function(v,i){if(!v)e.push(i)});if(e.length){a[e[Math.floor(Math.random()*e.length)]]=Math.random()<.9?2:4}}function reset(){a=Array(16).fill(0);sc.textContent=0;add();add();draw();b.focus()}function line(row,rev){var vals=row.filter(Boolean);if(rev)vals.reverse();for(var i=0;i<vals.length-1;i++)if(vals[i]===vals[i+1]){vals[i]*=2;sc.textContent=+sc.textContent+vals[i];vals.splice(i+1,1)}while(vals.length<4)vals.push(0);return rev?vals.reverse():vals}function move(dir){var old=a.join(',');for(var r=0;r<4;r++){var row=a.slice(r*4,r*4+4);if(dir==='left')a.splice(r*4,4,...line(row,false));if(dir==='right')a.splice(r*4,4,...line(row,true))}if(dir==='up'||dir==='down')for(var c=0;c<4;c++){var col=[a[c],a[c+4],a[c+8],a[c+12]];col=line(col,dir==='down');for(var r=0;r<4;r++)a[r*4+c]=col[r]}if(a.join(',')!==old){add();draw();if(Math.max.apply(null,a)>+best.textContent)best.textContent=Math.max.apply(null,a)}}function draw(){b.innerHTML='';a.forEach(function(v){var x=document.createElement('div');x.textContent=v||'';x.style.cssText='aspect-ratio:1;border-radius:8px;background:var(--surface-alt-color,#202633);display:grid;place-items:center;font-size:clamp(1.3rem,5vw,2rem);font-weight:800';b.appendChild(x)})}b.onkeydown=function(e){if(['ArrowLeft','ArrowRight','ArrowUp','ArrowDown'].indexOf(e.key)>=0){e.preventDefault();move(e.key.slice(5).toLowerCase())}};start.onclick=reset;reset()})()</script>